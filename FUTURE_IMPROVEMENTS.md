daytona improvements:

Limited number of ips/subnets whitelisting for outbound request.
Streaming of specific application window(or entire desktop) from computer use sandbox.
Lack of standalone repository which showcases real power of daytona sandbox.
Audio streaming in computer use sandbox.
Desktop streaming over better protocol.
Computer use functionality does not work smoothly(stop api, scroll api returning 504 and flickering effect on calling it).

library for one os keyboard to another os keyboard compatibility.
lack of docs around computer use(all keys, etc).

Android VM.
lack of examples with doc or use cases doc, full fledged repo in all languages for demonstration and marketing.
runtime snapshot.
golang sdk(already being developed).
